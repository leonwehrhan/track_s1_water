import numpy as np


def map_w_to_index(trj_file, top_file, w_names, index_file, verbose=False):
    '''
    Map water name to index in each frame of reduced trajectory.

    This function maps the indices of
    '''
